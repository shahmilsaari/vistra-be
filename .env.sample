# Application Configuration
PORT=4000
NODE_ENV=development

# Storage Configuration
STORAGE_BASE_URL=http://localhost:4000

# Database Configuration
# Format: mysql://username:password@host:port/database
# For local MySQL with no password: mysql://root@127.0.0.1:3306/vistra
DATABASE_URL=mysql://root@127.0.0.1:3306/vistra

# MySQL Connection Details (if needed separately)
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=vistra

# JWT Configuration
JWT_SECRET=change-me-to-a-secure-random-string
JWT_EXPIRES_IN=3600s
